export default function InfoPage() {
  return (
    <main className="space-y-6 max-w-[600px] m-auto">
      <h1 className="text-2xl font-bold">Information page</h1>
      <p>
        On each infomation page, we can have a chat which is loaded with all the
        context about this product / article
      </p>
    </main>
  );
}
